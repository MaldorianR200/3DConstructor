import { environment } from './environments/environment';

export const PRODUCTION: boolean = environment.production;
export const API_VERSION: string = environment.apiVersion;
export const BASE_URL: string = environment.apiUrl;
export const BASE_URL_STATIC: string = environment.apiUrlStatic;
